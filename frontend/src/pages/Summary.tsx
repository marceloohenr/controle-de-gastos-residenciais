import { Empty,ErrorMessage,PageHeader,Skeleton,StatCard } from '../components/UI';import { useAsync } from '../hooks/useAsync';import { api } from '../services/api';import { money } from '../utils/format';
export function SummaryPage(){const {data,loading,error}=useAsync(api.summary,[]);return <><PageHeader eyebrow="Consolidado" title="Totais da casa"/><section className="stats">{loading?<Skeleton/>:<><StatCard label="Total de receitas" value={money(data?.totals.totalIncome??0)} tone="income"/><StatCard label="Total de despesas" value={money(data?.totals.totalExpenses??0)} tone="expense"/><StatCard label="Saldo líquido" value={money(data?.totals.netBalance??0)} tone="balance"/></>}</section><section className="panel"><div className="panel-title"><div><span>Detalhamento</span><h2>Resultado por pessoa</h2></div></div>{loading?<Skeleton/>:error?<ErrorMessage message={error}/>:data?.people.length?<div className="table-wrap"><table><thead><tr><th>Pessoa</th><th>Receitas</th><th>Despesas</th><th>Saldo</th></tr></thead><tbody>{data.people.map(p=><tr key={p.personId}><td><strong>{p.personName}</strong></td><td className="positive">{money(p.totalIncome)}</td><td className="negative">{money(p.totalExpenses)}</td><td><strong className={p.balance<0?'negative':'positive'}>{money(p.balance)}</strong></td></tr>)}</tbody><tfoot><tr><td>Total geral</td><td>{money(data.totals.totalIncome)}</td><td>{money(data.totals.totalExpenses)}</td><td>{money(data.totals.netBalance)}</td></tr></tfoot></table></div>:<Empty title="Sem dados para consolidar" text="Cadastre pessoas e transações para visualizar os totais."/>}</section></>}
